import ResetPasswordPage from '@/components/reset-password'
import React, { Suspense } from 'react'

function page() {
  return (
    <Suspense>
    <ResetPasswordPage/>
    </Suspense>
  )
}

export default page